{
	"type": "module",
	"scripts": {
		"build:ffx": "vite build && vite build -c vite.content.config.js && node scripts/copy-manifest.mjs",
		"build:chrome": "vite build && vite build -c vite.content.config.js && node scripts/copy-manifest-chrome.mjs",
		"dev": "vite build --watch && node scripts/copy-manifest.mjs"
	},
	"devDependencies": {
		"vite": "^7.3.1"
	},
	"dependencies": {
		"@js-temporal/polyfill": "^0.5.1"
	}
}
